# ============================================================
# Flask 配置
# ============================================================
SECRET_KEY=change-this-to-a-random-string
PORT=5000
FLASK_ENV=development

# CORS（前端地址，多个用逗号分隔）
CORS_ORIGINS=http://localhost:5000,http://127.0.0.1:5000

# ============================================================
# Supabase Cloud 配置
# 在 Supabase Dashboard → Settings → API 获取
# ============================================================
# Project URL（例如: https://abcdefg.supabase.co）
SUPABASE_URL=https://your-project.supabase.co
# Service Role Key（以 eyJ 开头，注意：使用 service_role 而不是 anon key）
SUPABASE_KEY=eyJxxxxx

# ============================================================
# Clerk 用户认证配置
# 在 https://clerk.com 创建应用后获取以下值
# ============================================================
# Clerk Domain（例如: helpful-macaw-1.clerk.accounts.dev）
CLERK_DOMAIN=your-clerk-domain.clerk.accounts.dev
# Clerk Publishable Key（以 pk_ 开头，用于前端）
CLERK_PUBLISHABLE_KEY=pk_test_xxxxx
# Clerk Webhook Signing Secret（在 Clerk Dashboard → Webhooks 获取，以 whsec_ 开头）
CLERK_WEBHOOK_SECRET=whsec_xxxxx

# ============================================================
# 积分配置
# ============================================================
SIGNUP_BONUS_CREDITS=50
